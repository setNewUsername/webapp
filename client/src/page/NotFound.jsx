import React, {useContext} from 'react';
import {Context} from "../index";

const NotFound = () => {
    return (
        <div>
           Not found 404
        </div>
    );
};

export default NotFound;